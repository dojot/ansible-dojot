apiVersion: v1
kind: ServiceAccount
metadata:
  name: consul-consul-server
  namespace: {{ dojot_namespace }}
  labels:
    app: consul
    chart: consul-helm
    heritage: Helm
    release: consul
    component: server