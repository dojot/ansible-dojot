apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kong-audit
  namespace: {{ dojot_namespace }}
  labels:
    app: dojot
    name: kong-audit
spec:
  selector:
    matchLabels:
      app: dojot
      name: kong-audit
  accessModes:
    - ReadWriteOnce
  storageClassName: {{ dojot_storage_class_name }}
  resources:
    requests:
      storage: 5Gi
