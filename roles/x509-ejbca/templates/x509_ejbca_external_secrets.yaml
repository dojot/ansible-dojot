apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: x509-ejbca-secret
  namespace: {{ dojot_namespace }}
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: dojot-secret-store
    kind: SecretStore
  target:
    name: x509-ejbca-secret
    creationPolicy: Owner
    deletionPolicy: Delete
  dataFrom:
  - extract:
      key: secret/x509-ejbca
      version: "1"
