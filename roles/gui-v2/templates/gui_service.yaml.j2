apiVersion: v1
kind: Service
metadata:
  labels:
    name: gui-v2
  name: gui-v2
  namespace: {{ dojot_namespace }}
spec:
  ports:
  - name: "http"
    port: 80
    targetPort: 80
  selector:
    name: gui-v2
    app: dojot
