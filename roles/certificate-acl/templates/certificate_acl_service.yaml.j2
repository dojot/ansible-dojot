apiVersion: v1
kind: Service
metadata:
  labels:
    name: certificate-acl
  name: certificate-acl
  namespace: {{ dojot_namespace }}
spec:
  ports:
  - port: {{ dojot_certificate_acl_port }}
    targetPort: {{ dojot_certificate_acl_port }}
  selector:
    name: certificate-acl
    app: dojot
