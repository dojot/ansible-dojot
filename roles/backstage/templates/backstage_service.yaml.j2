apiVersion: v1
kind: Service
metadata:
  labels:
    name: backstage
  name: backstage
  namespace: {{ dojot_namespace }}
spec:
  ports:
  - port: {{ backstage_port }}
    targetPort: {{ backstage_port }}
  selector:
    name: backstage
    app: dojot