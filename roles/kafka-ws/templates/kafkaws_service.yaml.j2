apiVersion: v1
kind: Service
metadata:
  labels:
    name: kafkaws
  name: kafkaws
  namespace: {{ dojot_namespace }}
spec:
  ports:
  - port: 5000
    targetPort: 5000
  selector:
    name: kafkaws
    app: dojot
