apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: kafka-ws-secret
  namespace: {{ dojot_namespace }}
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: dojot-secret-store
    kind: SecretStore
  target:
    name: kafka-ws-secret
    creationPolicy: Owner
    deletionPolicy: Delete
  dataFrom:
  - extract:
      key: secret/kafka-ws
      version: "1"