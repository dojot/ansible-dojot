apiVersion: v1
kind: Service
metadata:
  labels:
    name: data-broker
  name: data-broker
  namespace: {{ dojot_namespace }}
spec:
  ports:
  - port: 80
    targetPort: 80
  selector:
    name: data-broker
    app: dojot
