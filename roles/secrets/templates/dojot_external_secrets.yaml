apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: dojot-secrets
  namespace: {{ dojot_namespace }}
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: dojot-secret-store
    kind: SecretStore
  target:
    name: dojot-secrets
    creationPolicy: Owner
    deletionPolicy: Delete
  dataFrom:
  - extract:
      key: secret/dojot-secrets
      version: "1"