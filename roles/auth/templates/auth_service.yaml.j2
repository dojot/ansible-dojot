apiVersion: v1
kind: Service
metadata:
  labels:
    name: auth
  name: auth
  namespace: {{ dojot_namespace }}
spec:
  type: NodePort
  ports:
  - port: 5000
    targetPort: 5000
    nodePort: 30005
  selector:
    name: auth
    app: dojot
