apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: x509-identity-pvc
  namespace: dojot
  labels:
    type: local
spec:
  storageClassName: local-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 4G